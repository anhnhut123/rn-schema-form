import { CommonProps } from "schema-form-core";
export { ArrayField };
declare function ArrayField({
  name,
  registry,
  schema,
  idSchema,
  uiSchema,
  required,
  readonly,
  disabled,
  idPrefix,
  idSeparator,
  methods,
  placeholder,
}: CommonProps): JSX.Element;
