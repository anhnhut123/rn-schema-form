import { CommonProps } from "schema-form-core";
export { NumberField };
declare function NumberField({
  name,
  schema,
  uiSchema,
  required,
  registry,
  methods,
  ...rest
}: CommonProps): JSX.Element;
